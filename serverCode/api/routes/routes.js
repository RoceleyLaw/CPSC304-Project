'use strict';
//export by default
module.exports = function (app) {
    
    // EXAMPLES: the coding example have been given below as guidelines
    // grab 
    // var strain = require('../controllers/controller');
    // var siteRoot = require('../routes/routes');


    // Strains
    // app.route('/strains')
    //     .get(strain.getAllItems);
}